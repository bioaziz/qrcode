# PDF — Summary

- Purpose: Share a PDF file with an optional landing page (card/minimal/cover), password gate, and open behavior.
- Fields: url, title, desc, company, site, cta, target (newtab|sametab|download), fonts (tf,bf), style, accent, accent2, cover (optional), password hash (ph optional).
- Preview: `/view/pdf?...` (see lib/preview.js builder for full param set).

