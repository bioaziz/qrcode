# MP3 — Summary

- Purpose: Share an audio file with optional social link buttons and password gate.
- Fields: url, title, desc, site, cta, target, fonts, style, accent, accent2, cover, ph, up to 6 social links `s{i}p`/`s{i}u`/`s{i}t`.
- Preview: `/view/mp3?...` (see lib/preview.js builder).

