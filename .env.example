GOOGLE_TRANSLATE_API_KEY=
# API key for POST /api/v1/qrcode and for signing GET URLs
API_KEY=

# Enable app-level GeoIP lookup fallback (uses local geoip-lite DB if installed)
# 0 = disabled (default), 1 = enabled
GEO_LOOKUP=0
